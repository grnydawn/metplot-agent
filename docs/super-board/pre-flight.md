# super-board pre-flight — metplot-agent

Generated by `super-board lint`. Each `[ ]` is a **halt gate**: `super-board run`
refuses to start until every item is `[✓]`. Flip a box to `[✓]` once satisfied.

> Last run: 2026-06-16. Scope: issue **#34** (user-selected).
> The board has **no issues in active-pipeline columns** (Ready / Building / QA /
> Review); #34 and #33 sit in **Backlog**. #34 was linted (ACs added); #33 was
> left un-linted by user choice. The gates below cover the repo's baseline dev
> tooling plus the specific tools #34's acceptance criteria exercise.

## 🔑 Credentials the loop will need

   (none — #34 is a local MCP-schema fix; no API keys or test logins referenced)

## 🛠 Tools the loop will need

   [✓] gh CLI authenticated (verified — account grnydawn)
   [✓] Python ≥ 3.10 (verified — 3.14.6 in ./.venv)
   [✓] Test/build deps installed in ./.venv (Python 3.14.6) — verified 2026-06-16:
       pytest 9.1.0, ruff, mypy, click all importable; `python -m tools.build`
       runs; `pytest tests/ --collect-only` collects the suite.
       Installed via: `.venv/bin/python -m pip install -e ".[dev,mcp]"`.
       NOTE: workers must run test/build/lint commands through the venv
       (`.venv/bin/python -m pytest …`, `.venv/bin/python -m tools.build …`)
       or with `. .venv/bin/activate` first — base python3 lacks these deps.
   [✓] MCP / plotting deps — xarray 2026.4.0, netCDF4 1.7.4, numpy 2.4.6,
       matplotlib 3.11.0, cartopy 0.25.0, mcp 1.27.2 — all importable in ./.venv.
       Required by #34 to run the servers and validate `list_tools` schemas
       (AC #1/#2) and `plot-renderer.*` calls (AC #1/#5).
   [✓] GitHub Copilot CLI 1.0.63 at /opt/homebrew/bin/copilot (verified) —
       needed for #34 AC #5 smoke test (the originally-reported environment).

## 🌐 Environment

   [✓] Git repo present, remote = grnydawn/metplot-agent, base branch = master
   [✓] Working tree clean (only untracked super-board docs)
   [✓] `netcdf-reader` server imports, lists 14 tools (verified)
   [✓] `plot-renderer` server imports, lists 4 tools: render_map,
       render_timeseries, render_profile, render_section (verified)
   [✓] Test fixture present: /tmp/sample.nc — readable NetCDF,
       temperature(time=3, plev=4, lat=19, lon=72) (verified; ideal for the
       inspect + timeseries smoke test in #34 AC #2/#5).
       NOTE: tests/ ships 0 `.nc` fixtures; the suite generates data
       programmatically — AC #2/#5 should use /tmp/sample.nc or a generated file.

**Status: all gates `[✓]` — `super-board run` is clear to start for #34.**
