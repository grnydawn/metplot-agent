# build outputs
build/
dist/
*.egg-info/
*.skill

# python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
uv.lock

# editors
.vscode/
.idea/
*.swp
.DS_Store

# project-local refinement drafts (private to each user's environment)
.metplot/

# secrets
.env
.env.test
*.local

# data — don't accidentally commit large NetCDF samples
*.nc
*.nc4
!tests/fixtures/*.nc

# Claude Code local config
.claude/

# pinned-real-files integration fixtures (gitignored, populated by download_samples.sh)
tests/mcp/netcdf_reader/integration/data/

# plot-renderer real-files developer config (local paths only)
tests/integration/real_files.json
.scratch/
