[project]
name = "ncplot-netcdf-reader"
version = "0.1.0"
description = "MCP server: NetCDF inspection and slicing"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.0",
  "xarray>=2024.1",
  "netcdf4>=1.6",
  "cftime>=1.6",
  "numpy>=1.24",
]
