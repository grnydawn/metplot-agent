[project]
name = "ncplot-plot-renderer"
version = "0.1.0"
description = "MCP server: matplotlib/cartopy figure rendering"
requires-python = ">=3.10"
dependencies = [
  "mcp>=1.0",
  "numpy>=1.24",
  "matplotlib>=3.8",
  "cartopy>=0.22",
]
