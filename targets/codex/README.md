# Codex target

Build with:

```
python -m tools.build codex
```

Output: `build/codex/ncplot/`.

Generates an `AGENTS.md` document from the canonical skills plus a setup
script for the MCP server dependencies. Status: stub. Hooking into the
OpenAI Agents SDK / Codex CLI for tool registration is TODO.
